<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      To change the default size or layout margins of the speech recognition 'View's and hint labels
      re-define the dimensions here.
      Dimensions 'button_record_size' and 'button_record_margin_end' affects the size or layout
      margins of the record button and the speech recognition button, since speech recognition
      button is set relative to the record button.

      Note:
      You don't have to re-define the dimension here if you're not changing it.
      Dimensions here are re-defined to the same default values just for example.
     -->

    <dimen name="button_record_size">72dp</dimen>
    <dimen name="button_record_margin_end">52dp</dimen>
    <dimen name="hint_label_height">52dp</dimen>
    <dimen name="hint_label_margin_end">12dp</dimen>
</resources>